# Go Price Calculator

This Go project reads a list of prices from a file, calculates tax-included prices for multiple tax rates concurrently using goroutines, and saves the results as JSON files. It demonstrates file handling, interfaces, concurrency, and basic data processing in Go.
